import { KilometersPerHourPipe } from './kilometers-per-hour.pipe';

describe('KilometersPerHourPipe', () => {
  it('create an instance', () => {
    const pipe = new KilometersPerHourPipe();
    expect(pipe).toBeTruthy();
  });
});
